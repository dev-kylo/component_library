import { newSpecPage } from '@stencil/core/testing';
import {UserLogin} from './user-login';

describe('<user-login>', () => {

    it('should look for a token in Local Storage upon mount', async () => {

    })

    it('the form should submit username+password fields to a function', async () => {

    })

    it('should fetch a new token if there is no token in local storage', async () => {

    })

    it('should set the error state if the fetch function returns incorrect username or password', async () => {

    })

})