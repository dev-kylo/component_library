import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';

# `<scroll-observer>`

<Meta
  title="Wrappers/Scroll Observer"
  component="scroll-observer"
  argTypes={{
    animation: {
      control: 'text',
      description: 'Set the animation class name',
    },
    lazyimage: {
      control: 'boolean',
      description: 'Legacy function.If this component is for making an image lazy.',
      table: { defaultValue: { summary: false } },
    },
  }}
/>

### Copy

```html
    <scroll-observer>

    //{ HTML } 

</scroll-observer>
```

### Description
This will wrap an Observer, using the browser's Intersection Observer API, to observe the wrapped children scrolling through the viewport. When it reaches the viewport, an animation fires.
The default animation is to fade in.


## All Controls


<Description markdown={readme} />

