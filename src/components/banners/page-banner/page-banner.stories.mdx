import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';

# `<page-banner>`

<Meta
  title="Other UI/Page Banner"
  component="page-banner"
  argTypes={{
    content: { type: 'text' },
    pagetitle: {
      type: 'string',
      description: 'The <h1> heading for a page. There should only be one pagetitle for page. Use the heading attribute for other titles.',
    },
    heading: {
      type: 'string',
      description: 'The <h2> heading for the section',
    },
    text: {
      type: 'text',
      description: 'The paragraph text. If you need text links or separate paragraphs, rather insert HTML.',
    },
    colorscheme: {
      type: 'string',
      description: 'The colours for the background & text of <project-heading>, separated with a comma. eg "text colour, bg colour"',
      table: { defaultValue: { summary: "white, #502669" } },
    },
    pagetitlecolours: {
      type: 'string',
      description: 'The colours for the <h1> heading, separated with a comma. eg "h1 colour, bg colour"',
      table: { defaultValue: { summary: "white, #1BA39C" } },
    },
    bgimage: {
      type: 'string',
      description: 'Supply an image for the banner',
    },
    image: {
      type: 'string',
      description: 'Supply a background image for the banner',
    },
    imagewidth: {
      control: {
        type: 'range',
        min: 50,
        max: 1200,
        step: 10,
      },
      description: 'Supply an custom width for the banner image',
    },
    imageheight: {
      control: {
        type: 'range',
        min: 20,
        max: 678,
        step: 2,
      },
      description: 'Supply an custom height for the banner image',
    },
    video: {
      type: 'string',
      description: 'Supply a video URL',
    },
    landing: {
      type: 'boolean',
      description: 'If a landing page',
    },
  }}
/>

## Build your Own

<Canvas withToolbar={true} withSource="open" args={{
  landing: false,
  pagetitle: '',
  content: '',
  heading: '',
  text: '',
  colourscheme: '',
  pagetitlecolours: '',
  bgimage: '',
  image: '',
  imagewidth: '',
  imageheight: '',
  video: '',
}}>
  <Story name="Build Your Own" component="page-banner">
    {args => html`<page-banner
      ?landing="${args.landing}"
      pagetitle="${ifDefined(args.pagetitle || undefined)}"
      heading="${ifDefined(args.heading || undefined)}"
      text="${ifDefined(args.text || undefined)}"
      colourscheme="${ifDefined(args.colourscheme || undefined)}"
      pagetitlecolours="${ifDefined(args.pagetitlecolours || undefined)}"
      bgimage="${ifDefined(args.bgimage || undefined)}"
      image="${ifDefined(args.image || undefined)}"
      imagewidth="${ifDefined(args.imagewidth || undefined)}"
      imageheight="${ifDefined(args.imageheight || undefined)}"
      video="${ifDefined(args.video || undefined)}"
    >${args.content||'My Banner'}</page-banner>`}
  </Story>
</Canvas>

<ArgsTable story="Build Your Own"/>

## More Info

<Description markdown={readme} />
