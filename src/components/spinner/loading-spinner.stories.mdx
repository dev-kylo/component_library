import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';

<Meta
  title="Loading Spinner"
  component="loading-spinner"
  argTypes={{
    show: {
      control: 'boolean',
      description: 'Display the spinner',
    },
    margin: {
      control: 'text',
      description: 'Set margin of spinner',
      table: { defaultValue: { summary: 'auto' } }
    },
  }}
/>

# `<loading-spinner>`

## Content

To set the button's text, use the `text` attribute, or simply add text as a child of the element. If you set the `text` attribute as well as adding child text, the attribute value will prefix the contents.

<!-- TODO: implement icon -->
<!-- To add an icon to the button, use the `icon` attribute -->

### Example

### Try It

<Canvas withSource="open" >
  <Story name="Spinner" component="loading-spinner" args={{
    show: true,
    margin: 'auto',
  }}>
    {args => html`
      <loading-spinner
        ?show="${ifDefined(args.show)}"
        margin="${ifDefined(args.margin || undefined)}"
      ></loading-spinner>`}
  </Story>
  <ArgsTable story="Spinner"/>
</Canvas>

## More Info

<Description markdown={readme} />
