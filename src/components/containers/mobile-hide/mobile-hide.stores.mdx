import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';
import dedent from 'ts-dedent';

<Meta
  title="Wrappers/Hide on Mobile"
  component="desktop-hide"
  argTypes={{
    hidetablet: { type: 'boolean', description: 'In addition to hiding all children content in screens mobile, this will hide content in screens tablet + smaller' },
  }}
/>

# `<mobile-hide>`

### Example

```html
<mobile-hide> Something I don't want to show on mobile devices</mobile-hide>
```

### Try It

<Canvas withSource="open" >
  <Story name="Build Your Own" component="mobile-hide" args={{
    content: 'Contents',
    hidetablet: false
  }}>
    {args => html`
      <mobile-hide
        ?hidetablet="${args.hidetablet}"
      >${args.content}</mobile-hide>
    `}
  </Story>
</Canvas>

<ArgsTable story="Build Your Own"/>

## More Info

<Description markdown={readme} />
