import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';
import dedent from 'ts-dedent';

<Meta
  title="Layouts/Flex"
  component="flex-container"
  argTypes={{
    alignx: { type: 'radio', description:'Set the X axis alignment. Use CSS Flex justify-content values', options: ['center', 'end', 'start', 'space-around', 'space-between']},
    aligny: { type: 'radio', description: 'Set the Y axis alignment. Use CSS Flex align-items values', options: ['baseline', 'center', 'end', 'start', 'stretch']},
    direction: { type: 'radio', description:'Same as the CSS Flex flow properties.',  options: ['column', 'column-reverse', 'row', 'row-reverse']},
    fillcontainer: { type: 'boolean', description: 'Ensure the container stretches the full width and height of parent container' },  
    wrap: { type: 'boolean', description: 'Elements will wrap, and instead of squeezing onto one line they will go one under another when there is no more space.' }
  }}
/>

# `<flex-container>`

### Used for
If you need to center something on a page, or have 2+ things lined up next to each other.

### Example

```html
<flex-container alignx="center">

  <!-- HTML elements go here -->

</flex-container>
```

### Try It

<Canvas withSource="open" >
  <Story name="Basic" component="flex-container" args={{
    alignx: 'center',
    aligny: 'center',
  }}>
    {args => html`
<flex-container
    alignx="${ifDefined(args.alignx || undefined)}"
    aligny="${ifDefined(args.aligny || undefined)}">
  <div style="margin: 1rem; width:250px; height: 100px; background: red"></div>
</flex-container>
    `}
  </Story>
</Canvas>

<ArgsTable story="Basic" include={ ['alignx', 'aligny']}/>


<Canvas withSource="open" >
  <Story name="Advanced-2" component="flex-container" args={{
    alignx: 'space-around',
    aligny: 'center',
    direction: 'row',
    fillcontainer: true,
    wrap: true,
  }}>
    {args => html`
<flex-container
    alignx="${ifDefined(args.alignx || undefined)}"
    aligny="${ifDefined(args.aligny || undefined)}"
    direction="${ifDefined(args.direction || undefined)}"
    ?fillcontainer="${args.fillcontainer}"
    ?wrap="${args.wrap}">
  <div style="margin: 1rem; width:250px; height: 100px; background: red"></div>
  <div style="margin: 1rem; width:250px; height: 100px; background: red"></div>
</flex-container>
    `}
  </Story>
</Canvas>

<ArgsTable story="Advanced-2"/>


<Canvas withSource="open" >
  <Story name="Advanced-3" component="flex-container" args={{
    alignx: 'space-around',
    aligny: 'center',
    direction: 'row',
    fillcontainer: true,
    wrap: true,
  }}>
    {args => html`
<flex-container
    alignx="${ifDefined(args.alignx || undefined)}"
    aligny="${ifDefined(args.aligny || undefined)}"
    direction="${ifDefined(args.direction || undefined)}"
    ?fillcontainer="${args.fillcontainer}"
    ?wrap="${args.wrap}"
    ?renderlist="${args.renderlist}">
    
  <div style="margin: 1rem; width:150px; height: 100px; background: red"></div>
  <div style="margin: 1rem; width:150px; height: 100px; background: red"></div>
  <div style="margin: 1rem; width:150px; height: 100px; background: red"></div>
  
</flex-container>
    `}
  </Story>
</Canvas>

<ArgsTable story="Advanced-3"/>

## More Info

<Description markdown={readme} />
