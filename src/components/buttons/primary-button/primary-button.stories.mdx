import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';

# `<primary-button>`

<Meta
  title="Buttons/Primary"
  component="primary-button"
  argTypes={{
    content: { type: 'text' },
    to: { type: 'text' }
  }}
/>

<Canvas isColumn={true} withSource={true} isExpanded={true}>
  <Story name="Primary Button" component="primary-button" args={{ to: '', content: 'Button' }}>
    {args => html`
      <primary-button to="${args.to}">${args.content}<primary-button/>
    `}
  </Story>
  <ArgsTable story="Primary Button"/>
</Canvas>

## More Info

<Description markdown={readme} />
