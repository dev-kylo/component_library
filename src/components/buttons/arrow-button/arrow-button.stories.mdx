import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';

# `<arrow-button>`

<Meta
  title="Buttons/Arrow"
  component="arrow-button"
  argTypes={{
    purple: {
      control: 'boolean',
      description: 'Make exit button purple',
      table: { defaultValue: { summary: false } },
    },
    direction: { 
      description: 'Set the direction of the arrow button (and the side of the containing element it appears on).',
      control: {
        type:'select',
      },
      options: ['left', 'right', 'up', 'down']
    },
    callback: { 
        action: 'clicked',
        description: 'A callback function for the button clicked'
    } 
  }}
/>

### Example

```html
<arrow-button purple></arrow-button>
```

<!-- TODO: implement icon -->
<!-- To add an icon to the button, use the `icon` attribute -->


### Build your own
<Canvas withToolbar={true} withSource="open" >
  <Story name="Basic" component="arrow-button" args={{
    purple: true,
    direction: 'left'
  }}>
    {args => html`
      <arrow-button
        purple="${ifDefined(args.purple || undefined)}"
        direction="${ifDefined(args.direction || undefined)}"
      ></arrow-button>`}
  </Story>
</Canvas>

<ArgsTable story="Basic" /> }/>

## Instructions
This button is used for sliders or any UI which requires navigation. 

It takes any callback function, which is attached to the click listener.

### Appearance

There is one option to customize the button's appearance.
Set the `purple` attribute to use the secondary colour pallete. 


<Description markdown={readme} />

