import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';

# `<video-embed>`

<Meta
  title="Media/Video"
  component="video-embed"
  argTypes={{
    embedid: {
      type: 'string',
      description: 'The id of the Youtube Video - found in the URL eg youtube.com/watch?v={VIDEO_ID}',
    },
  }}
/>

## Build your Own

<Canvas withToolbar={true} withSource="open">
  <Story name="Basic" component="video-embed" args={{
  embedid: 'DNT6t6yLldM',
  }}>
    {args => html`<video-embed
      embedid="${ifDefined(args.embedid || undefined)}"
    >${args.content}</video-embed>`}
  </Story>
</Canvas>

<ArgsTable story="Basic"/>

## More Info

<Description markdown={readme} />
