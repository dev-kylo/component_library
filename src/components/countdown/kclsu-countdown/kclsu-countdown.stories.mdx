import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs/blocks';

import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined.js';

import readme from './readme.md';

# `<kclsu-countdown>`

<Meta
  title="Buttons/Countdown"
  component="kclsu-countdown"
  argTypes={{
    date: {
      control: 'date',
      description: 'The date to countdown to. Must match date string format.',
    },
    text: {
      control: 'text',
      description: 'The text beneath the countdown',
    },
    textcolour: {
      control: 'text',
      description: 'Optional: set a colour for the text beneath the countdown.',
    },
    fontsize: {
      control: 'text',
      description: 'Optional: set a fixed font-size for tablet, desktop and wider',
    },
    width: { 
        control: 
            { type: 'range',
             min: 60, 
             max: 400, 
             step: 20 }, 
        description: 'The width of the countdown clock - in Pixels ONLY' 
    },
    margin: {
      control: 'text',
      description: 'Add margin to the countdown',
    },
    purple: {
      control: 'boolean',
      description: 'Give the message text a white block background.',
      table: { defaultValue: { summary: false } },
    }
  }}
/>

### Example

```html
    <kclsu-countdown date='Sepember 20, 2022, 11:00:00'></kclsu-countdown>
```


### Build your own
<Canvas withToolbar={true} withSource="open" >
  <Story name="Basic" component="kclsu-countdown" args={{
    date: 'Sepember 20, 2022, 11:00:00',
    text: 'Until something happens'
  }}>
    {args => html`
      <kclsu-countdown
        date="${ifDefined(args.link || undefined)}"
        text="${ifDefined(args.link || undefined)}"
      ></kclsu-countdown>`}
  </Story>
</Canvas>

<ArgsTable story="Basic" include={ ['date', 'text'] }/>

## Instructions
To set the button's text, use the `text` attribute, or simply add text as a child of the element. If you set the `text` attribute as well as adding child text, the attribute value will prefix the contents.

Using the component controls listed above, you can do the following:
 
- Create a primary standard teal button
- Create a secondary purple button
- control the size - big or small
- center the button in the page

### Appearance

There are four options to customize the button's appearance.
Set the `purple` attribute to use the secondary colour pallete. Set the `rounded` attribute to give the button rounded corners. Set the `small` or `verysmall` attributes to decrease the button's size.



## All Controls


<Description markdown={readme} />

